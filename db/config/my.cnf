[mysqld]
# 어디서든 접근 가능하게 설정 (요청사항 반영)
bind-address = 0.0.0.0

# 문자 인코딩 설정 (요청사항 반영)
character-set-server = utf8mb4
collation-server = utf8mb4_general_ci

# 추가 설정 (선택 사항이지만 권장)
skip-name-resolve

# 새로 추가된 설정: 시간대를 서울(Asia/Seoul)로 설정
default-time-zone = '+09:00'